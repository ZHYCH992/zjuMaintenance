import ReactDOM from 'react-dom/client';
import App from './App.jsx';

const root = document.createElement('div');
root.id = 'zjuMaintenance';
document.body.append(root);
ReactDOM.createRoot(root).render(<App />);
